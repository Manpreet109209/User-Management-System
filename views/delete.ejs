<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Delete User</title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    *, *::before, *::after {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    }

    .card {
      position: relative;
      background: #fff;
      max-width: 420px;
      width: 100%;
      padding: 3.5rem 2rem 2rem; /* Extra top padding for back button */
      border-radius: 24px;
      box-shadow: 0 25px 50px rgba(0,0,0,0.2);
      text-align: center;
      transition: transform 0.25s ease, box-shadow 0.25s ease;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 35px 70px rgba(0,0,0,0.25);
    }

    /* Back button */
    .back {
      position: absolute;
      top: 1rem; /* Adjusted */
      left: 1rem;
      text-decoration: none;
    }

    .back-btn {
      border: none;
      cursor: pointer;
      padding: 0.55rem 1.1rem;
      border-radius: 999px;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      color: #333;
      font-size: 0.85rem;
      font-weight: 600;
      box-shadow: 0 6px 18px rgba(0,0,0,0.22);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .back-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 12px 26px rgba(0,0,0,0.32);
    }

    h3 {
      font-size: 1.25rem;
      font-weight: 600;
      color: #333;
      margin-bottom: 1.8rem;
      line-height: 1.5;
    }

    h3 span {
      color: #e63946;
      font-weight: 700;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 1.3rem;
    }

    input[type="password"] {
      padding: 0.75rem 1rem;
      border-radius: 14px;
      border: 1.5px solid #ddd;
      font-size: 1rem;
      transition: border 0.2s ease, box-shadow 0.2s ease;
    }

    input[type="password"]:focus {
      outline: none;
      border-color: #e63946;
      box-shadow: 0 0 0 5px rgba(230, 57, 70, 0.15);
    }

    button[type="submit"] {
      padding: 0.8rem;
      border-radius: 999px;
      border: none;
      background: linear-gradient(135deg, #ff4b4b, #e63946);
      color: #fff;
      font-size: 1rem;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 10px 22px rgba(0,0,0,0.25);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    button[type="submit"]:hover {
      transform: translateY(-2px);
      box-shadow: 0 15px 30px rgba(0,0,0,0.35);
    }

    button[type="submit"]:active {
      transform: translateY(0);
      box-shadow: 0 8px 16px rgba(0,0,0,0.25);
    }

    .note {
      margin-top: 1.5rem;
      font-size: 0.85rem;
      color: #777;
    }

    @media (max-width: 480px) {
      .card {
        padding: 3rem 1.5rem 2rem; /* Adjust top padding for mobile */
      }

      h3 {
        font-size: 1.1rem;
      }

      input[type="password"], button[type="submit"] {
        font-size: 0.95rem;
      }

      .back-btn {
        font-size: 0.8rem;
        padding: 0.45rem 1rem;
      }
    }
  </style>
</head>
<body>
  <div class="card" role="region" aria-label="Delete User Form">
    <a href="/user" class="back">
      <button class="back-btn">⟵ Back</button>
    </a>

    <h3>
      Delete user: <span><%= user.email %></span><br />
      Please enter your password to confirm
    </h3>

    <form method="post" action="/user/<%= user.id %>?_method=DELETE">
      <input
        type="password"
        name="password"
        placeholder="Enter your password"
        required
        aria-label="Password"
      />
      <button type="submit">Delete</button>
    </form>

    <p class="note">
      This action is permanent. No undo.
    </p>
  </div>
</body>
</html>